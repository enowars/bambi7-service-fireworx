CREATE TABLE IF NOT EXISTS
users(
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	name TEXT,
	p TEXT,
	q TEXT,
	g TEXT,
	y TEXT
);

create table if not exists
events(
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	userid INTEGER SECONDARY KEY,
	time TEXT,
	wish TEXT,
	x FLOAT,
	y FLOAT
);
